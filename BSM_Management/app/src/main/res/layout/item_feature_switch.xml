<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="64dp"
    android:padding="12dp"
    android:background="@android:color/white">

    <!-- Icon -->
    <ImageView
        android:id="@+id/imgFeatureIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:contentDescription="Icon"
        app:srcCompat="@drawable/ic_check_circle"
        app:tint="@color/bsm_success"/>

    <!-- Tiêu đề -->
    <TextView
        android:id="@+id/tvFeatureTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Tên tính năng"
        android:textStyle="bold"
        android:textColor="@color/bsm_text_primary"
        android:textSize="15sp"
        app:layout_constraintStart_toEndOf="@id/imgFeatureIcon"
        app:layout_constraintTop_toTopOf="@id/imgFeatureIcon"
        app:layout_constraintEnd_toStartOf="@id/swFeature"
        android:layout_marginStart="12dp" />

    <!-- Mô tả -->
    <TextView
        android:id="@+id/tvFeatureDesc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Mô tả tính năng"
        android:textColor="@color/bsm_text_secondary"
        android:textSize="13sp"
        app:layout_constraintStart_toStartOf="@id/tvFeatureTitle"
        app:layout_constraintTop_toBottomOf="@id/tvFeatureTitle"
        app:layout_constraintEnd_toEndOf="@id/tvFeatureTitle"/>

    <!-- Switch -->
    <com.google.android.material.materialswitch.MaterialSwitch
        android:id="@+id/swFeature"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>
